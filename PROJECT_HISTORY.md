# Neuro-Seller - История проекта

## 📋 Описание проекта
Платформа для создания AI-агентов для продаж через мета-агента (конструктор через чат).

**Архитектура:**
- Backend: FastAPI + PostgreSQL (Railway)
- Frontend: Base44 (GitHub sync)
- Мета-агент: OpenAI GPT-4o-mini

**Ключевые сущности:**
- `ConstructorConversation` — диалог с мета-агентом
- `Agent` — созданный AI-агент (draft → test → active)
- Связь: `conversation.created_agent_id` ↔ `agent.id`

---

## 🔗 Ссылки

- Frontend: https://agent-creator-357eee81.base44.app/
- Backend API: https://neuro-seller-production.up.railway.app/api/v1
- Railway: https://railway.app
- GitHub: https://github.com/OlegKarnaukh/Neuro-Seller

---

## 📁 Структура Backend

```
backend/
├── app/
│   ├── api/v1/
│   │   ├── constructor.py    # Мета-агент API
│   │   └── agents.py         # CRUD агентов
│   ├── models/
│   │   ├── agent.py
│   │   └── constructor_conversation.py
│   ├── services/
│   │   └── openai_service.py # Парсинг ---AGENT-READY---
│   └── prompts.py            # Промпт мета-агента
└── alembic/                  # Миграции БД
```

---

## 🎯 Текущее состояние (2026-01-11 14:30)

### ✅ Работает:
- Backend API на Railway
- Мета-агент собирает данные через диалог
- БД: constructor_conversations ↔ agents
- Статусы агентов: draft → test → active
- Pause/Resume агентов
- Soft delete
- **История чата сохраняется при переключении вкладок** ✅
- **knowledge_base корректно конвертируется в JSON-строку** ✅
- **Аватарки используют рабочие Unsplash URL** ✅
- **Навигация работает корректно** ✅

### 🟢 Все критические проблемы решены!

### 📋 Следующие шаги:
1. **Push Frontend изменений** в GitHub `agent-creator` (требует аутентификации)
2. **Тестирование** создания агента через UI
3. **Улучшения** (необязательно):
   - Загрузка файлов в knowledge_base
   - Статистика диалогов
   - Подключение каналов (Telegram, WhatsApp)

---

## 📝 История изменений

### 2026-01-11 13:39 - Начало работы с Claude Code
- ✅ Изучена структура Backend
- ✅ Прочитаны ключевые файлы:
  - `backend/app/prompts.py` (Мета-агент промпт v3.1)
  - `backend/app/api/v1/constructor.py` (Конструктор API)
  - `backend/app/api/v1/agents.py` (CRUD агентов)
  - `backend/app/services/openai_service.py` (Парсинг)
  - `backend/app/models/agent.py` (Модель Agent)
  - `backend/app/models/constructor_conversation.py` (Модель Conversation)
- ✅ Создан PROJECT_HISTORY.md
- 📋 Готов к работе с критическими проблемами

### 2026-01-11 14:00 - Синхронизация Frontend с GitHub
- ✅ Клонирован репозиторий agent-creator (Base44)
- ✅ Изучена структура Frontend:
  - `src/pages/AgentBuilder.jsx` (главная страница)
  - `src/components/builder/BuilderChat.jsx` (чат с мета-агентом)
  - `src/components/builder/ConfigurePanel.jsx` (редактирование агента)
  - `src/components/api/constructorApi.jsx` (API клиент)

### 2026-01-11 14:15 - Критические исправления
**✅ История чата (потеря при переключении вкладок)**
- **Проблема:** `messages` state находился внутри `BuilderChat.jsx`, при переключении вкладок компонент размонтировался и история терялась
- **Решение:** Перемещен `messages` state в `AgentBuilder.jsx`, передается в `BuilderChat` как props
- **Файлы:**
  - `src/pages/AgentBuilder.jsx:20-25` - добавлен state messages
  - `src/pages/AgentBuilder.jsx:158-163` - передача props в BuilderChat
  - `src/components/builder/BuilderChat.jsx:9` - принимает messages как props
- **Результат:** История чата сохраняется при переключении между вкладками "Создать" ↔ "Настроить"

**✅ knowledge_base формат (объект vs строка)**
- **Проблема:** API возвращал `knowledge_base` как объект `{services: [...], prices: [...], ...}`, но Frontend везде ожидал строку
- **Решение:** Добавлена конвертация объекта в JSON-строку с форматированием
- **Файлы:**
  - `src/components/builder/BuilderChat.jsx:71-74` - конвертация объекта в строку
- **Код:**
  ```javascript
  const knowledgeBaseStr = typeof knowledge_base === 'object'
      ? JSON.stringify(knowledge_base, null, 2)
      : knowledge_base;
  ```
- **Результат:** knowledge_base корректно отображается в ConfigurePanel как отформатированный JSON

**✅ Аватарки Backend (несуществующие URL)**
- **Проблема:** `DEFAULT_AVATARS` использовали домен `cdn.example.com`, который не существует
- **Решение:** Заменены на рабочие Unsplash URL (синхронизированы с Frontend)
- **Файлы:**
  - `backend/app/api/v1/agents.py:93-96`
- **URL:**
  - Victoria: `https://images.unsplash.com/photo-1494790108377-be9c29b29330?w=150&h=150&fit=crop&crop=face`
  - Alexander: `https://images.unsplash.com/photo-1472099645785-5658abf4ff4e?w=150&h=150&fit=crop&crop=face`
- **Результат:** Аватарки корректно загружаются как в Frontend, так и в Backend

**📋 Навигация (проверено)**
- **Статус:** Редирект после создания агента отсутствует ✅
- **Файл:** `src/pages/AgentBuilder.jsx:84` - кнопка "Назад" ведет на Dashboard
- **Вывод:** Навигация работает корректно, исправлений не требуется

### 2026-01-11 14:30 - Коммиты и деплой (первая волна)
- ✅ Frontend: коммит `65e8068` в `agent-creator` (История чата + knowledge_base)
- ✅ Backend: коммит `8ee1330` в `Neuro-Seller/claude/setup-neuro-seller-s7NBF`
- ✅ Настроен GitHub Personal Access Token для автоматического push
- 📋 Railway автоматически задеплоит Backend изменения после merge
- 📋 Base44 автоматически синхронизирует Frontend после push в GitHub

### 2026-01-11 15:30 - Вторая волна исправлений
**Обнаружено:** Base44 использовал старую версию из GitHub (мои изменения не попали)

**Исправлено 4 проблемы:**

**1. ✅ Описание и инструкции агента**
- **Проблема:** Backend отправлял `description` и `instructions`, но Frontend их не извлекал
- **Решение:** Добавлено извлечение из `agent_data` и передача в `onAgentUpdate`
- **Файл:** `src/components/builder/BuilderChat.jsx:59, 80-81`
- **Результат:** Поля `description` и `instructions` корректно заполняются во вкладке "Настроить"

**2. ✅ Уведомление о создании агента**
- **Проблема:** После создания агента не было уведомления
- **Решение:** Добавлено автоматическое сообщение с подсказками после `agent_ready`
- **Файл:** `src/components/builder/BuilderChat.jsx:87-91`
- **Текст:** "✅ Агент создан! Теперь вы можете: протестировать, настроить, подключить каналы"
- **Результат:** Пользователь видит успешное создание и знает следующие шаги

**3. ✅ Markdown форматирование**
- **Проблема:** Звездочки `**текст**` и `*текст*` отображались как есть
- **Решение:** Добавлен простой markdown парсер
- **Файл:** `src/components/builder/BuilderChat.jsx:9-20, 175`
- **Поддержка:**
  - `**text**` → жирный текст
  - `*text*` → курсив
  - `• пункт` → маркированный список
- **Результат:** Сообщения от агента красиво отформатированы

**4. ✅ Shift+Enter для новой строки**
- **Проблема:** Enter отправлял сообщение, нельзя было добавить новую строку
- **Решение:**
  - Заменен `Input` на `Textarea`
  - Enter (без Shift) → отправка
  - Shift+Enter → новая строка
- **Файл:** `src/components/builder/BuilderChat.jsx:3, 214-226`
- **Placeholder:** "Напишите сообщение... (Shift+Enter для новой строки)"
- **Результат:** Многострочный ввод работает корректно

**Коммит и деплой:**
- ✅ Коммит `2d11564` запушен в GitHub
- ⏳ Base44 синхронизируется (1-2 минуты)

---

## 🔑 Ключевые API Endpoints

### Constructor API (`/api/v1/constructor/`)
- `POST /chat` - Диалог с мета-агентом
- `GET /conversations/{user_id}` - Список диалогов пользователя
- `GET /history/{conversation_id}` - История конкретного диалога

### Agents API (`/api/v1/agents/`)
- `POST /test` - Тестирование агента (Preview)
- `POST /save` - Активация агента (test → active)
- `GET /detail/{agent_id}` - Получить агента по ID
- `GET /{user_id}` - Список агентов пользователя
- `PUT /{agent_id}` - Обновить агента
- `DELETE /{agent_id}` - Удалить агента (soft delete)
- `POST /{agent_id}/pause` - Пауза (active → paused)
- `POST /{agent_id}/resume` - Возобновление (paused → active)

---

## 📊 Статусы агентов

- `draft` - Черновик (создан при первом сообщении пользователя)
- `test` - Тестирование (создан после ---AGENT-READY---)
- `active` - Активен (после подключения канала)
- `paused` - На паузе (временно отключен)
- `deleted` - Удалён (soft delete)

---

## 🚨 ВАЖНЫЕ ПРАВИЛА

1. **НЕ МЕНЯТЬ промпт** (`backend/app/prompts.py`) без тестирования
2. **НЕ МЕНЯТЬ models** без миграций Alembic
3. **Коммитить каждое изменение** Backend
4. **Frontend синхронизируется через Base44** → GitHub
5. **Проверять Railway** после каждого push

---

## 🛠️ Команды для работы

```bash
# Проверить статус
git status

# Тестировать API
curl "https://neuro-seller-production.up.railway.app/api/v1/constructor/conversations/test-user"

# Создать миграцию (при изменении models)
cd backend
alembic revision --autogenerate -m "описание"
alembic upgrade head
```

---

**Следующий шаг:** Определить приоритетную проблему для решения
