# 🤖 Claude Workflow Checklist

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Проверка перед началом работы

### 1️⃣ Проверить текущую ветку и статус
```bash
git status
git branch -a
```

**Railway деплоит из ветки:** `main`
**Рабочая ветка Claude:** `claude/setup-neuro-seller-s7NBF`

### 2️⃣ Проверить что Railway видит изменения
```bash
# Сравнить текущую ветку с main
git log origin/main..HEAD --oneline

# Если есть коммиты - нужно создать PR!
```

### 3️⃣ Создать PR когда есть готовые изменения

**Вариант A: Автоматически через gh CLI (если настроен GH_TOKEN)**
```bash
export PATH="$HOME/.local/bin:$PATH"
gh pr create --base main --head $(git branch --show-current) --fill
```

**Вариант B: Автоматическая ссылка на создание PR**
```bash
BRANCH=$(git branch --show-current)
echo "🔗 Создать PR: https://github.com/OlegKarnaukh/Neuro-Seller/compare/main...$BRANCH"
```

**Вариант C: Попросить пользователя**
```
"✅ Готово X коммитов. Нужно смержить в main для деплоя на Railway.
Создайте PR: https://github.com/OlegKarnaukh/Neuro-Seller/compare"
```

---

## 📋 Чеклист перед коммитом

- [ ] Проверил что работаю в ветке `claude/setup-neuro-seller-s7NBF`
- [ ] Протестировал изменения локально (если возможно)
- [ ] Написал понятный commit message
- [ ] После пуша проверил что коммит попал в правильную ветку

---

## 📋 Чеклист после завершения фичи

- [ ] Все коммиты запушены в `claude/setup-neuro-seller-s7NBF`
- [ ] Создан PR в `main` или попросил пользователя смержить
- [ ] Проверил что Railway начал деплой после мержа
- [ ] Проверил логи деплоя на наличие ошибок

---

## 🚨 Частые ошибки (ИЗБЕГАТЬ!)

### ❌ Работа в main напрямую
```bash
# НИКОГДА не делать:
git checkout main
# работа...
git commit
git push  # ❌ 403 ошибка!
```

### ❌ Забыть проверить что Railway видит изменения
```bash
# Проблема: коммиты есть, но они в feature-ветке
# Railway не видит их, потому что следит за main
```

### ✅ Правильный workflow
```bash
# 1. Всегда в рабочей ветке
git checkout claude/setup-neuro-seller-s7NBF

# 2. Работаем
# ...делаем изменения...

# 3. Коммитим
git add .
git commit -m "feat: ..."

# 4. Пушим в рабочую ветку
git push -u origin claude/setup-neuro-seller-s7NBF

# 5. Проверяем сколько коммитов не в main
git log origin/main..HEAD --oneline

# 6. Если есть коммиты - создаём PR или говорим пользователю
```

---

## 🔧 Git команды для проверки

```bash
# Где я нахожусь?
git branch --show-current

# Сколько коммитов не в main?
git log origin/main..HEAD --oneline | wc -l

# Что изменилось?
git diff origin/main...HEAD --stat

# Обновить информацию о ветках
git fetch origin
```

---

**📅 Создано:** 2026-01-12
**🎯 Цель:** Избежать ситуации когда Railway не видит изменения из-за того что они в feature-ветке
